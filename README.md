# Introduction

This is a basic site set up with honkit.

To install, use:

```
npm install honkit
npm install
```

Then to run the site locally, use

```
npx honkit serve
```

or

```
npm run serve
```

